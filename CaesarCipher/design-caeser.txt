Program Design1. What does this component do in a single sentence?Take a string, and encrypt/decrypt it by iterating through every character and modifying it through adding/subtracting a key and wrapping it around the alphabet.1. What is the interface to this component? What are its inputs and outputs?This component takes in a string, and returns a list of encryptions or decryptions based on the value of the key from 0-25.1. What are five representative test cases this component should pass?2. - a null string3. -a string with one letter4. -numbers5. -symbols-a regular string (“HElloWOrld”)