1. What does this component do in a single sentence?The Vignere Cipher uses a keyword to encrypt or decrypt the string, which means instead of a fixed value, it depends on the integer representations of the chaaracters in the keyword string.2. What is the interface to this component? What are its inputs and outputs?The component takes in a keyword and string, and outputs the encoded or decoded string.3. What are five representative test cases this component should pass?-a null keyword and a regular string-a null keyword and a null string-a string keyword and a string -a numerical keyword and a string-a symbolic keyword and a string